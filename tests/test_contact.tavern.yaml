test_name: phonebook service test

stages:
  - name: Make sure we can add new contact
    request:
      url: http://localhost:9301/v2/contact
      method: POST
      json:
        
    response:
      status_code: 200
